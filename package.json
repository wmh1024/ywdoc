{
  "name": "yunweidocs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs --port 3001",
    "clog": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "sass": "^1.75.0",
    "vitepress": "^1.0.0-alpha.4",
    "vue": "^3.4.23"
  }
}
